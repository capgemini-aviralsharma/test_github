$ git --version $ git config --global user.name "Ritika Balodi" $ git config --global user.email "ritika.balodi@capgemini.com" $ git config user.name $ git config --list $ git help $ cd c:/Users/RBALODI/MyFirstRepository  // change directory $ git init $ls $ cd.. //come out of repository $ echo "#MyFirstRepository" >> README.md $ git add . // added to local repository $ git status $ git commit -m "first commit" $ git remote add origin https://github.com/RitikaBalodi26/MyFirstRepoGit.git $ git push -u origin master
